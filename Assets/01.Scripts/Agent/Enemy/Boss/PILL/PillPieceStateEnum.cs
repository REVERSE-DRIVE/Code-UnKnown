public enum PillPieceStateEnum {
    Idle,
    TargetFollow,
    TargetRush,
    CureAttack,
    LeaserAttack,
    Disband, // 해제
    Equip //장착
}

public enum PillBodyStateEnum {
    Idle,
    ShockWave,
    LaserAttack
}